package com.akinfopark.savingsApp.Utils;

import android.widget.CompoundButton;

public interface ItemViewCheckChangeListener {
    void onChange(CompoundButton compoundButton, boolean isChecked, int i);
}
